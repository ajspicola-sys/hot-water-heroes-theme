# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Node
node_modules/

# Logs
*.log

# Deploy helper with secret key - keep local only
assets/deploy.php

# One-shot dev/migration scripts — never commit these (a script with SSH
# credentials was once committed here; keep all local tooling out of git)
assets/*.py
assets/*.ps1
